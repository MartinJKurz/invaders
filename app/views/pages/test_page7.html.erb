<!DOCTYPE html>
<html>
  <head>
    <title>Puzzle - MOUSE</title>
    <!-- script src="/mootools-core-1.4.0-full-compat-yc.js" type="text/javascript"></script-->
    <script src="/mootools-core-1.4.0-full-compat.js" type="text/javascript"></script>
    <script src='/log.js' type='text/javascript'></script>
    <script src='/public_helpers.js' type='text/javascript'></script>
    <script src="/test_page7.js" type="text/javascript"></script>
    <meta name="viewport" content="width=device-width, user-scalable=no">
    <style type="text/css">
      body {
        margin: 0px; padding: 0; overflow: hidden;
      }
      /* disable text selection */
      * {
        -webkit-user-select: none;
        -khtml-user-select: none;
        -moz-user-select: none;
        -o-user-select: none;
        user-select: none;
      }
      html {
        height: 100%;
      }
      body {
        height: 100%;
        margin: 0;
        background-repeat: no-repeat;
        background-attachment: fixed;
        background: red;  /* testing - this value must be overridden */
        background: -o-linear-gradient(left, #444, #fff, #444);
        /*
        background: -o-radial-gradient( 50% 50%, circle, #fff 0px, #444 600px );
        background: -o-linear-gradient(top, #c8482c, #fff);
        background: -o-radial-gradient(center, #fff, #adf);
        */

        /* background: -webkit-gradient( radial, 50% 50%, 0, 50% 50%, 600, from(#fff), to(#444) ); */
        /* background: -moz-radial-gradient( 50% 50%, circle, #fff 0px, #444 600px ); */
      }
      .menu-button {
        border: none;
        background-color: rgba(0,0,0,0);
        width: 100%;
      }
      .menu-button:hover {
        background-color: rgba(255,0,0,0.5);
      }
      .menu-button:focus {
        background-color: rgba(0,128,255,0.5);
      }
      .menu-button:active {
        background-color: rgba(255,128,0,0.5);
      }
      /*
      button::-moz-focus-inner {
        border: 0;
      }
      */
      input[type="submit"]::-moz-focus-inner, input[type="button"]::-moz-focus-inner {
        border : 0px;
      }



      /* canvas { border: 1px solid black; } */
      /*button { border: 1px solid red; padding: 0; margin: 0;} */
    </style>

  </head>
  <body onload="onLoad()">
    <canvas style="position: absolute" id="canvas"></canvas>
  </body>
</html>

