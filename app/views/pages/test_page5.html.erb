<!DOCTYPE html>


<html>
  <head>
    <title>Puzzle</title>

    <!-- PRODUCTION % DEVELOPMENT -->
    <%= javascript_include_tag "test_page5_content", :debug => Rails.env.development? %>


    <META http-equiv="Default-Style" content="semi_transparent">
    <%= stylesheet_link_tag 'fullscreen', :media => 'screen' %>
    <%= stylesheet_link_tag 'menus', :media => 'screen', :rel => 'stylesheet' %>

    <!-- user style specific -->
    <!-- standard style -->
    <%= stylesheet_link_tag 'menus_standard', :media => 'screen', :title => 'standard', :rel => 'alternate stylesheet' %>

    <!-- hip style -->
    <%= stylesheet_link_tag 'menus_hip', :media => 'screen', :title => 'hip', :rel => 'alternate stylesheet' %>

    <!-- transparent style -->
    <%= stylesheet_link_tag 'menus_transparent', :media => 'screen', :title => 'transparent', :rel => 'alternate stylesheet' %>

    <!-- semi transparent style -->
    <%= stylesheet_link_tag 'menus_semi_transparent', :media => 'screen', :title => 'semi_transparent', :rel => 'alternate stylesheet' %>

    <!-- transparent_white style -->
    <%= stylesheet_link_tag 'menus_transparent_white', :media => 'screen', :title => 'transparent_white', :rel => 'alternate stylesheet' %>

    <!-- negative style -->
    <%= stylesheet_link_tag 'menus_negative', :media => 'screen', :title => 'negative', :rel => 'alternate stylesheet' %>

    <!-- outline style -->
    <%= stylesheet_link_tag 'menus_outline', :media => 'screen', :title => 'outline', :rel => 'alternate stylesheet' %>


    <meta name="viewport" content="width=device-width, user-scalable=no">
    <!--
    <style type="text/css">
      body {
        margin: 0px; padding: 0; overflow: hidden;
      }
      /* disable text selection */
      * {
        -webkit-user-select: none;
        -khtml-user-select: none;
        -moz-user-select: none;
        -o-user-select: none;
        user-select: none;
      }
      html {
        height: 100%;
      }
      body {
        height: 100%;
        margin: 0;
        background-repeat: no-repeat;
        background-attachment: fixed;
        background: red;  /* testing - this value must be overridden */
        background: -o-linear-gradient(left, #444, #fff, #444);
        /*
        background: -o-radial-gradient( 50% 50%, circle, #fff 0px, #444 600px );
        background: -o-linear-gradient(top, #c8482c, #fff);
        background: -o-radial-gradient(center, #fff, #adf);
        */

        /* background: -webkit-gradient( radial, 50% 50%, 0, 50% 50%, 600, from(#fff), to(#444) ); */
        /* background: -moz-radial-gradient( 50% 50%, circle, #fff 0px, #444 600px ); */
      }
      .menu-button {
        border: none;
        background-color: rgba(0,0,0,0);
        width: 100%;
      }
      .menu-button:hover {
        background-color: rgba(255,0,0,0.5);
      }
      .menu-button:focus {
        background-color: rgba(0,128,255,0.5);
      }
      .menu-button:active {
        background-color: rgba(255,128,0,0.5);
      }
      /*
      button::-moz-focus-inner {
        border: 0;
      }
      */
      input[type="submit"]::-moz-focus-inner, input[type="button"]::-moz-focus-inner {
        border : 0px;
      }



      /* canvas { border: 1px solid black; } */
      /*button { border: 1px solid red; padding: 0; margin: 0;} */
    </style>
    -->

  </head>
  <body onload="onLoad()">
    <canvas style="position: absolute" id="canvas"></canvas>
  </body>
</html>

